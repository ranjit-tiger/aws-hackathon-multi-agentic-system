# Clone repository
git clone https://github.com/ranjit-tiger/aws-hackathon-multi-agentic-system.git
cd aws-hackathon-multi-agentic-system

# Setup and run
`uv init`
`uv venv`
`source .venv/bin/activate`
`uv sync`
`uv run python -m app.main`
